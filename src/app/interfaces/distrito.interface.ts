export interface Distrito {
  idDistrito: number;
  nombreDistrito: string;
  // Puedes añadir más campos si tu entidad Distrito en Java los tiene, por ejemplo:
  // provincia?: { idProvincia: number; nombreProvincia: string; };
}